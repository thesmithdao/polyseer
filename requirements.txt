uagents>=0.16.0
uagents-core>=0.1.0
httpx>=0.27.0
# LLM backends (only the one you select via LLM_PROVIDER is used at runtime):
openai>=1.40.0       # used for LLM_PROVIDER=asi1 (ASI:One is OpenAI-compatible) and =openai
anthropic>=0.40.0    # used for LLM_PROVIDER=anthropic
